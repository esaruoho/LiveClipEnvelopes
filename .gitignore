.DS_Store
*.o
# shortcuts/unsigned/ IS tracked: the .wflow files are the editable source that
# tools/set-launcher.py rewrites, and shortcuts/signed/*.shortcut is built FROM them.
# Ignoring the source while tracking the output is how they nearly got lost.
*.zip
gumroad/.rendered.html
gumroad/.preview.png
__pycache__/
*.pyc
keyboard-maestro/local/

# built by install.sh --with-arrows (source is bin/live-envelope-arrows.swift)
bin/live-envelope-arrows
